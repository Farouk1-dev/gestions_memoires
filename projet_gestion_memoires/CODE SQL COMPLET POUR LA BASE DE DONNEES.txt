CODE SQL DE TOUT MA BASE DE DONNÉÉS

CREATION BASE DE DONNEES


CREATE DATABASE gestion_memoires;

USE gestion_memoires;



TABLE UTILISATEURS


CREATE TABLE utilisateurs (

    id INT AUTO_INCREMENT PRIMARY KEY,

    nom VARCHAR(100) NOT NULL,

    email VARCHAR(150) NOT NULL UNIQUE,

    telephone VARCHAR(20),

    password VARCHAR(255) NOT NULL,

    role ENUM(
        'admin',
        'professeur',
        'etudiant'
    ) NOT NULL,

    photo VARCHAR(255) DEFAULT 'default.png'

);



TABLE MEMOIRES


CREATE TABLE memoires (

    id INT AUTO_INCREMENT PRIMARY KEY,

    utilisateur_id INT NOT NULL,

    titre VARCHAR(255) NOT NULL,

    categorie VARCHAR(100) NOT NULL,

    resume TEXT,

    fichier VARCHAR(255) NOT NULL,

    statut ENUM(
        'brouillon',
        'en_attente',
        'valide',
        'retourne'
    ) DEFAULT 'brouillon',

    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,

    FOREIGN KEY (utilisateur_id)
    REFERENCES utilisateurs(id)
    ON DELETE CASCADE

);


TABLE COMMENTAIRES


CREATE TABLE commentaires (

    id INT AUTO_INCREMENT PRIMARY KEY,

    utilisateur_id INT NOT NULL,

    memoire_id INT NOT NULL,

    commentaire TEXT NOT NULL,

    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,

    FOREIGN KEY (utilisateur_id)
    REFERENCES utilisateurs(id)
    ON DELETE CASCADE,

    FOREIGN KEY (memoire_id)
    REFERENCES memoires(id)
    ON DELETE CASCADE

);



TABLE LIKES


CREATE TABLE likes (

    id INT AUTO_INCREMENT PRIMARY KEY,

    utilisateur_id INT NOT NULL,

    memoire_id INT NOT NULL,

    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,

    FOREIGN KEY (utilisateur_id)
    REFERENCES utilisateurs(id)
    ON DELETE CASCADE,

    FOREIGN KEY (memoire_id)
    REFERENCES memoires(id)
    ON DELETE CASCADE

);



TABLE NOTIFICATIONS


CREATE TABLE notifications (

    id INT AUTO_INCREMENT PRIMARY KEY,

    utilisateur_id INT NOT NULL,

    titre VARCHAR(255) NOT NULL,

    message TEXT NOT NULL,

    statut ENUM(
        'lu',
        'non_lu'
    ) DEFAULT 'non_lu',

    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,

    FOREIGN KEY (utilisateur_id)
    REFERENCES utilisateurs(id)
    ON DELETE CASCADE

);



TABLE SOUMISSIONS


CREATE TABLE soumissions (

    id INT AUTO_INCREMENT PRIMARY KEY,

    memoire_id INT NOT NULL,

    etudiant_id INT NOT NULL,

    professeur_id INT NOT NULL,

    commentaire TEXT,

    statut ENUM(
        'en_attente',
        'valide',
        'retourne'
    ) DEFAULT 'en_attente',

    date_soumission TIMESTAMP DEFAULT CURRENT_TIMESTAMP,

    FOREIGN KEY (memoire_id)
    REFERENCES memoires(id)
    ON DELETE CASCADE,

    FOREIGN KEY (etudiant_id)
    REFERENCES utilisateurs(id)
    ON DELETE CASCADE,

    FOREIGN KEY (professeur_id)
    REFERENCES utilisateurs(id)
    ON DELETE CASCADE

);



TABLE CONTACT ADMIN


CREATE TABLE contact_admin (

    id INT AUTO_INCREMENT PRIMARY KEY,

    nom VARCHAR(100) NOT NULL,

    email VARCHAR(150) NOT NULL,

    sujet VARCHAR(255) NOT NULL,

    message TEXT NOT NULL,

    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP

);



EXEMPLE ADMIN


INSERT INTO utilisateurs (

    nom,
    email,
    telephone,
    password,
    role,
    photo

)

VALUES (

    'Administrateur',

    'admin@gmail.com',

    '01919191',

    '$2y$10$fZkC9VA9AH4PfLzwb.MCI.LdmBooIdQvr9afDpy4IzOQ4W8k9Q7yK',

    'admin',

    'default.png'

);



/*
Pour vous connecter sur login.php utiliser

Email : admin@gmail.com
Mot de passe : admin123
*/